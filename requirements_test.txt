# Test/CI dependencies for the composed notary (installed into the engine uv venv).
# Keep the runtime pins in one place; setup-bonsai-27b.sh installs this file directly.
-r requirements_notary.txt
pytest==9.1.0
